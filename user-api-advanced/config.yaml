server:
  port: "8080"

database:
  host: "localhost"
  port: "5432"
  user: "postgres"
  # password 敏感信息，通过环境变量 DB_PASSWORD 覆盖，禁止在此填写真实密码
  password: ""
  name: "go_user_api"

jwt:
  # secret 通过环境变量 JWT_SECRET 覆盖，禁止在生产环境使用此默认值
  secret: "change-me-in-production"
  expire: "24h"
